# Copy to .dev.vars for local `wrangler dev` / `npm run preview` / python-script
ADMIN_PASSWORD=your_admin_password
BUNNY_API_KEY=your_library_access_key
# Prefer workers.dev for Publish-as-links sync (custom domains may block script UAs)
SITE_URL=https://shortfilmsite.YOUR_SUBDOMAIN.workers.dev
